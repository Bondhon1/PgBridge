{
  "name": "pgbridge",
  "version": "1.0.0",
  "description": "Offline-ready PostgreSQL migration desktop tool",
  "main": "src/main.js",
  "author": "",
  "license": "MIT",
  "scripts": {
    "start": "electron .",
    "generate:icon": "node scripts/generate-icons.js",
    "build": "npm run generate:icon && electron-builder --win"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "electron": "^35.3.0",
    "electron-builder": "^24.13.3",
    "png-to-ico": "^3.0.1",
    "sharp": "^0.34.5"
  },
  "build": {
    "appId": "com.pgbridge.app",
    "productName": "PgBridge",
    "icon": "assets/icon.ico",
    "directories": {
      "output": "dist"
    },
    "files": [
      "src/**/*",
      "ui/**/*",
      "assets/**/*",
      "Untitled design.svg",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "resources/postgres",
        "to": "postgres",
        "filter": [
          "**/*"
        ]
      }
    ],
    "win": {
      "icon": "assets/icon.ico",
      "target": [
        "nsis"
      ]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    }
  }
}
